<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd">

  <!-- Exercises SpringContextFactory's legacy 'plugin:' protocol resolver: one location that resolves
       through the bundle (proving the resolver works) and one that doesn't (proving the resolver's
       'not found' warning path is safe and simply falls through to the default resource loading). -->
  <bean id="props" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
    <property name="ignoreResourceNotFound" value="true"/>
    <property name="locations">
      <list>
        <value>plugin:some/nested/spring-context-factory-found.properties</value>
        <value>plugin:spring-context-factory-missing.properties</value>
      </list>
    </property>
  </bean>

</beans>
